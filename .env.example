# Copy to .env and fill in.
#
# The demo runs entirely on MotherDuck (cloud DuckDB, free tier). No card
# required. Get a token from https://motherduck.com → Settings → Access
# Tokens (read/write).

MOTHERDUCK_TOKEN=eyJ…
